  <%= render_list :class => "nav nav-tabs" do |li|
                li << link_to("本週新片",movies_path)
                li << link_to("票房排行",movies_path)
                li << link_to("單項評比排行",movies_path)
              end %>

<table class="table">
  <tr>
    <th>Title</th>
    <th>Released</th>
    <th>Genre</th>
    <th>Director</th>
    <th>Writer</th>
    <th>Star</th>
    <th>Summary</th>
    <th>Rating</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @movies.each do |movie| %>
  <tr>
    <td><%= movie.title %></td>
    <td><%= movie.released %></td>
    <td><%= movie.genre %></td>
    <td><%= movie.director %></td>
    <td><%= movie.writer %></td>
    <td><%= movie.star %></td>
    <td><%= movie.summary %></td>
    <td><%= rating_for movie %></td>
    <td><%= link_to 'Show', movie %></td>
    <td><%= link_to 'Edit', edit_movie_path(movie) %></td>
    <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Movie', new_movie_path %>
